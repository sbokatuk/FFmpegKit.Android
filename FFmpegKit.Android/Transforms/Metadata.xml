<metadata>
  <!--
  This sample removes the class: android.support.v4.content.AsyncTaskLoader.LoadTask:
  <remove-node path="/api/package[@name='android.support.v4.content']/class[@name='AsyncTaskLoader.LoadTask']" />
  
  This sample removes the method: android.support.v4.content.CursorLoader.loadInBackground:
  <remove-node path="/api/package[@name='android.support.v4.content']/class[@name='CursorLoader']/method[@name='loadInBackground']" />
  -->
  <attr path="/api/package[@name='com.arthenica.ffmpegkit']" name="managedName">Ffmpegkit.Droid</attr>

  <!--
    FFmpegKit 5.1 exposed AbstractSession's backing fields (createTime, endTime, returnCode, ...)
    which collided with the properties generated from their getters, so they were removed here.
    As of 8.1.7 those fields are no longer visible to class-parse and the rules matched nothing,
    so they have been dropped rather than left emitting BG8A00 warnings on every build.
  -->

  <!-- rename getName to GetAbiName so that when it becomes a parameter it does not become Name which conflicts with Enum.Name -->
  <attr path="/api/package[@name='com.arthenica.ffmpegkit']/class[@name='Abi']/method[@name='getName']" name="managedName">GetAbiName</attr>

</metadata>
